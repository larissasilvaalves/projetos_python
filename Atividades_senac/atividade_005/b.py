import os 

os.system ('cls')

inicio = int(input('entre com o valor inicial: '))
fim = int(input('entre com o numero final: '))


for i in range (inicio , fim + 1):

    print(f'os valores são {i}')
